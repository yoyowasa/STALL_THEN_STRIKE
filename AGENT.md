## Language
- 以後の説明・要約・レビュー・コミット文は **日本語** で。
- 変数名・関数名・API名などの識別子は英語のままでよい。
- 出力は簡潔に。根拠を短く添える。
- コードへ付与するコメントも日本語で。

## Workspace Safety
- 編集前に必ず現在地（`Get-Location`）と対象ファイルの絶対パスを確認する。
- 前後の編集で親フォルダ（リポジトリルート）が変わる場合は、作業を止めてユーザーに確認するまで編集しない。
- 絶対パス指定が会話に出ても、作業基準は現在の指定ワークスペースを優先し、切替はユーザーの明示指示がある時のみ行う。
